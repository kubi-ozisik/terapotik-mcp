{
  "name": "@terapotik/shared",
  "version": "0.1.0",
  "description": "Shared package for Terapotik, types, utilities and database schema",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "postinstall": "prisma generate"
  },
  "devDependencies": {
    "prisma": "^6.14.0"
  },
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "google-auth-library": "^10.2.1",
    "googleapis": "^148.0.0",
    "zod": "^4.1.1"
  }
}
